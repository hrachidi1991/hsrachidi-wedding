import '../admin.css';

export const metadata = {
  title: 'Hostess Sign In · Hussein & Suzan',
  robots: { index: false, follow: false },
};

export default function SorelleLayout({ children }: { children: React.ReactNode }) {
  return children;
}
